services:
  ner-sllm-api:
    build: .
    container_name: ner-sllm-api
    ports:
      - "8005:8005"
    volumes:
      - ./models:/app/models
    environment:
      - TZ=Asia/Seoul
    restart: unless-stopped